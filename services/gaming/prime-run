#!/bin/sh
# NVIDIA PRIME Render Offload Script
# Forces applications to use NVIDIA GPU on hybrid graphics systems
# Usage: prime-run <application>

exec env __NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia "$@"
